/**
 * Stan Jay Universal Transaction Format (SJ-UTF)
 *
 * The internal standard format for all business transactions.
 * Every source converts TO SJ-UTF.
 * Every destination imports FROM SJ-UTF.
 */
export {};
